openapi: 3.0.0

info:
  title: Warehouse API
  description: API for managing warehouses
  version: 1.1.0

paths:
  /warehouse:
    get:
      summary: List warehouse units
      operationId: listWarehouseUnits
      parameters:
        - name: location
          in: query
          description: Filter warehouses by location
          required: false
          schema:
            type: string
        - name: limit
          in: query
          description: Maximum number of results to return
          required: false
          schema:
            type: integer
            format: int32
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Warehouse'

    post:
      summary: Create a new warehouse unit
      operationId: createWarehouseUnit
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WarehouseCreate'
      responses:
        '200':   # ← IMPORTANT: match your implementation
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Warehouse'
        '422':
          description: Validation error

  /warehouse/{businessUnitCode}:
    get:
      summary: Get warehouse unit by businessUnitCode
      operationId: getWarehouseUnitByBusinessUnitCode
      parameters:
        - name: businessUnitCode
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Warehouse'
        '404':
          description: Not Found

    delete:
      summary: Delete warehouse unit by businessUnitCode
      operationId: deleteWarehouseUnitByBusinessUnitCode
      parameters:
        - name: businessUnitCode
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: No Content
        '404':
          description: Not Found

components:
  schemas:
    Warehouse:
      type: object
      required:
        - businessUnitCode
        - location
        - capacity
      properties:
        businessUnitCode:
          type: string
        location:
          type: string
        capacity:
          type: integer
          format: int32
        stock:
          type: integer
          format: int32

    WarehouseCreate:
      type: object
      required:
        - businessUnitCode
        - location
        - capacity
      properties:
        businessUnitCode:
          type: string
        location:
          type: string
        capacity:
          type: integer
          format: int32
        stock:
          type: integer
          format: int32
